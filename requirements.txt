# Streamlit app dependencies. Upper bounds prevent silently deploying a
# breaking major release on Streamlit Cloud (it installs on every reboot).
streamlit>=1.32,<2
pandas>=1.5,<3
requests>=2.28,<3
openpyxl>=3.1,<4
